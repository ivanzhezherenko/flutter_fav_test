import 'package:flutter/material.dart';

class MovieItemDescription extends StatelessWidget {
  const MovieItemDescription({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [],
    );
  }
}
